import { createWithEqualityFn } from "zustand/traditional";

export default createWithEqualityFn((setState) => ({
	name: "",
	setName: (name) => setState({ name }),
	teamName: "",
	setTeamName: (teamName) => setState({ teamName }),
	selectedOrganization: { _id: "", name: "" },
	setSelectedOrganization: (org) => setState({ selectedOrganization: org }),
	repoName: "",
	setRepoName: (repoName) => setState({ repoName }),
	userName: "",
	setUserName: (userName) => setState({ userName }),
	showGlobalLoading: false,
	setShowGlobalLoading: (showGlobalLoading) => setState({ showGlobalLoading }),
	branchName: "",
	setBranchName: (branchName) => setState({ branchName }),
	fileName: "",
	setFileName: (fileName) => setState({ fileName }),
	showNotificationBadge: false,
	apiTitle: "",
	setApiTitle: (apiTitle) => setState({ apiTitle }),
	setShowNotificationBadge: (showNotificationBadge) => setState({ showNotificationBadge }),
	qualityViolations: {
		Category: "",
		Critical: true,
		Major: true,
		Minor: true,
	},
	setQualityViolations: (qualityViolations) => setState({ qualityViolations }),
	shouldFetch: true,
	setShouldFetch: (shouldFetch) => setState({ shouldFetch }),
	notification: {},
	setNotification: (notification) => setState({ notification }),
	shortView: "",
	setShortView: (shortView) => setState({ shortView }),
}));
